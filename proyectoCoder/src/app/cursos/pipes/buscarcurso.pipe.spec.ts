import { BuscarcursoPipe } from './buscarcurso.pipe';

describe('BuscarcursoPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarcursoPipe();
    expect(pipe).toBeTruthy();
  });
});
